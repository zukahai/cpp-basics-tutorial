#include <bits/stdc++.h>

using namespace std;

int main() {
    // int n = 12345;
    // string s = to_string(n);
    // s += "abc";
    // cout << s;

    // vector<int> v;
    // v.push_back(1);
    // v.push_back(2);
    // v.push_back(3);

    // reverse(v.begin(), v.end());

    // for (int i = 0; i < v.size(); i++) {
    //     cout << v[i] << " ";
    // }

    string s = "Gia Vinh";
    cout << s.substr(4, 2); // substr(indexStart, length)
}
#include <bits/stdc++.h>
#include <map>

using namespace std;
int a[1000007];
map<long long, int> dem;

int main() {
    // int n;
    // cin >> n;
    // for (int i = 0; i < n; i++) {
    //     cin >> a[i];
    // }

    // for (int i = 0; i < n; i++) {
    //     dem[a[i]]++;
    // }

    // int res = -1;
    // int max = INT_MIN;
    // for (iterator::map<int, int>::iterator it = dem.begin(); it != dem.end(); it++) {
    //     if (it->second > max) {
    //         max = it->second;
    //         res = it->first;
    //     }
    // }

    // cout << res << endl;

    cout << 2 % 3;
    
}